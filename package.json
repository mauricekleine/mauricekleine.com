{
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --filter @mauricekleine/web build && node apps/web/tools/link-deploy-config.mjs",
    "deploy": "bunx wrangler deploy",
    "dev": "bun run --filter @mauricekleine/web dev",
    "prepare": "husky",
    "test": "node apps/web/tools/test-worker.mjs"
  },
  "lint-staged": {
    "package.json": [
      "bun x sort-package-json"
    ]
  },
  "devDependencies": {
    "husky": "~9.1.7",
    "lint-staged": "~16.2.7",
    "sort-package-json": "~3.5.0"
  },
  "packageManager": "bun@1.4.2"
}
